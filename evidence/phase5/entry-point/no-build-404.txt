/README.txt -> 404 application/json; charset=utf-8 {"ok":false,"status":404,"reason":"/README.txt is not built: this relay serves the beacon out of the directory named by RELAY_BEACON_DIR, which holds no README.txt. The operator rebuilds it with: npm run beacon:build -- --deployment <file> --origin <this origin> --out <that directory>. The relay's own endpoints are unaffected: /health.json, /me/<address>.json, /proposals.json, /state.json, /pending.json, /relay"}
/llms.txt -> 404 application/json; charset=utf-8 {"ok":false,"status":404,"reason":"/llms.txt is not built: this relay serves the beacon out of the directory named by RELAY_BEACON_DIR, which holds no llms.txt. The operator rebuilds it with: npm run beacon:build -- --deployment <file> --origin <this origin> --out <that directory>. The relay's own endpoints are unaffected: /health.json, /me/<address>.json, /proposals.json, /state.json, /pending.json, /relay"}
/robots.txt -> 404 application/json; charset=utf-8 {"ok":false,"status":404,"reason":"/robots.txt is not built: this relay serves the beacon out of the directory named by RELAY_BEACON_DIR, which holds no robots.txt. The operator rebuilds it with: npm run beacon:build -- --deployment <file> --origin <this origin> --out <that directory>. The relay's own endpoints are unaffected: /health.json, /me/<address>.json, /proposals.json, /state.json, /pending.json, /relay"}
/snippet.js -> 404 application/json; charset=utf-8 {"ok":false,"status":404,"reason":"/snippet.js is not built: this relay serves the beacon out of the directory named by RELAY_BEACON_DIR, which holds no snippet.js. The operator rebuilds it with: npm run beacon:build -- --deployment <file> --origin <this origin> --out <that directory>. The relay's own endpoints are unaffected: /health.json, /me/<address>.json, /proposals.json, /state.json, /pending.json, /relay"}
/snippet.py -> 404 application/json; charset=utf-8 {"ok":false,"status":404,"reason":"/snippet.py is not built: this relay serves the beacon out of the directory named by RELAY_BEACON_DIR, which holds no snippet.py. The operator rebuilds it with: npm run beacon:build -- --deployment <file> --origin <this origin> --out <that directory>. The relay's own endpoints are unaffected: /health.json, /me/<address>.json, /proposals.json, /state.json, /pending.json, /relay"}
/CONSTITUTION.md -> 404 application/json; charset=utf-8 {"ok":false,"status":404,"reason":"/CONSTITUTION.md is not built: this relay serves the beacon out of the directory named by RELAY_BEACON_DIR, which holds no CONSTITUTION.md. The operator rebuilds it with: npm run beacon:build -- --deployment <file> --origin <this origin> --out <that directory>. The relay's own endpoints are unaffected: /health.json, /me/<address>.json, /proposals.json, /state.json, /pending.json, /relay"}
/index.html -> 404 application/json; charset=utf-8 {"ok":false,"status":404,"reason":"/index.html is not built: this relay serves the beacon out of the directory named by RELAY_BEACON_DIR, which holds no index.html. The operator rebuilds it with: npm run beacon:build -- --deployment <file> --origin <this origin> --out <that directory>. The relay's own endpoints are unaffected: /health.json, /me/<address>.json, /proposals.json, /state.json, /pending.json, /relay"}
/ -> 404 application/json; charset=utf-8 {"ok":false,"status":404,"reason":"/ is not built: this relay serves the beacon out of the directory named by RELAY_BEACON_DIR, which holds no index.html. The operator rebuilds it with: npm run beacon:build -- --deployment <file> --origin <this origin> --out <that directory>. The relay's own endpoints are unaffected: /health.json, /me/<address>.json, /proposals.json, /state.json, /pending.json, /relay"}
