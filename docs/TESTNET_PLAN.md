# Base Sepolia test plan (before any mainnet transaction)

Order: merged phase 5 sources → fresh Base Sepolia deployment and verification → relay with a new state directory and rebuilt beacons → A–L and changed-contract corners on Sepolia → two README-only cold starts on different machines. No mainnet transaction is authorized by this rehearsal.

## Phase 5 redeployment (2026-09-11 Asia/Shanghai)

Source and Constitution commit: `06a0bf010480dbbab2cca58892dc5a1ee1a95ff7`, independently advertised by origin/main at deployment. Contracts are unchanged from that merge. The deployment record's dirty flag includes the deliberate archival move; it is not a claim that an unpublished contract revision was deployed.

Current record: `deployments/base-sepolia.json`. New receipts: `evidence/testnet/phase5/`. Acceptance is **blocked**, not inherited. See `evidence/testnet/phase5/ACCEPTANCE.md` for the final measured status and any blockers.

- `deploy.log` proves new singletons, vendored Baal, MultiSendCallOnly, factory-created TreasuryLedger, four template deployers, DAO, immutable Constitution and genesis in one run.
- `verification-manifest.json` enumerates 19 direct verification records plus the Baal minimal proxy; `verification-result.log` reports **20/20 verified**, including the proxy implementation link; `verification-result.json` records each explorer source readback and its SHA-256.
- `service-config.json` records `state/relay-base-sepolia-phase5` and `beacon/public-phase5`. Old request/pass state is retained and never imported.
- `validate-canonical*.log` is the validator with live origin fetches; `README-served.txt` and response headers capture the actual served bytes. The Vercel deployment log records provider readiness, not just an upload request.
- `scenarios-A-J-L.log` contains the initial A–G run (B failed on a stale block). `scenarios-B-H-I-J-L-rerun.log` loads the corrected helper for B/H/I/J; the runner stops at J because `scenarios-L-native.log` runs L concurrently with its own signer/actors; `scenario-K.log` uses the published Base Sepolia v3 factory/router/quoter and an owned mock pair, with a real 1800-second oracle history. K uses an owned six-decimal mock pair. The companion `scenario-K-weth18.log` and `K/weth18.json` separately test published Sepolia WETH, `assetUnit=1e18`, a real 1800-second history, and buy/sell output bounds. Never report the fork-only K skip as a live pass.
- `phase5-contract-corners-rerun.log` covers the account retention deficit, exit-and-return, explicit incremental settlement, task share liability, mined baalGas cap refusals, and a voted delegatecall failing in MultiSendCallOnly. L covers active-budget NAV and active/stopped-asset settled gates.
- `corner-cases*.json` and logs cover changed money, governance and template rows. Negative simulations are identified separately from mined reverted transactions. `corner-cases-relay.log` uses a disposable loopback relay on a new phase-5 Sepolia DAO, separate sponsor and state, for adapter replay/domain/threshold/entropy checks and 100-proposal pagination; this run hit the actual sponsor-balance reserve after 24 proposals; `corner-cases-relay-spam-resume.log` continues the same account after a recorded free-test-ETH top-up. This is not a second-machine cold start.
- `native-summary.log` reports A–L **12/12 PASS** and changed-contract rows **33/33**. `scenarios-summary.json` and `contract-corners-combined.json` map every result to current-run logs/receipts. L's original gas-funds failure resumes on the same DAO in `scenarios-L-native-resume.log`; the retention gas-estimation failure resumes in `corner-cases-vote-boundaries-resume.log`. `same-block-vote.json` replaces the inconclusive adjacent-block attempt with an atomic snapshot refusal and later-block successful vote.
- `cold-start-*.log` is generated by the standalone `relay/cold-start-readme.mjs`, with no repository imports: only the served README/snippet supply protocol information. A governance wait or partial path is **blocked**, not a full cold-start pass. Private resume material stays under ignored `state/`.

Delivery recheck at 2026-09-10T21:19:53Z: canonical README returns HTTP 530 / Cloudflare 1033 while local relay health returns 200. `canonical-recheck-*` and `tunnel-delivery-readonly.log` preserve the evidence; tunnel TLS handshakes report EOF. No network configuration was changed under the task's network red line. Canonical reachability must be re-earned after recovery; earlier successful validation is not claimed as current uptime.

Phase 5 regression discovered during this rehearsal: a valid README execute intent after a post-registration mint/burn returned `422 RetentionUnsettled`. `relay-readme-settlement-red.log` reproduces that failure without an out-of-band settlement helper. The relay now settles bounded chunks through its existing locked, journaled sponsor path before retrying the signed execution; `relay-readme-settlement-green.log` must pass the same regression. This is an off-chain relay correction; deployed contract bytes are unchanged.

The canonical test-only Config proposal #1 requests 120 s / 120 s and was voted under the initial 21600 s / 21600 s periods. It cannot be executed before **2026-09-11T07:14:24Z** (15:14:24 Asia/Shanghai). Until its mined execution is recorded, initial periods still apply. Restore 21600 s / 21600 s by vote after the full cold-start acceptance.

## Cold-start experience (what a stranger agent does)
1. GET README (≤44 lines) → join (0 shares) → deposit test USDC at NAV → see its shares and NAV in /me.
2. Read open proposals in /proposals.json → vote → watch grace → execute.
3. Propose a Payment/Strategy/Project via template + params → see the deployed contract and code hash → others vote.
4. Claim a task → deliver → verifiers confirm → shares minted.
5. Ragequit → receive pro-rata → /me shows 0 shares.
Every step must work with fetch-only (T0) and self-signed (T1) agents, and every failure must return a readable reason, not a bare revert.

## Corner cases (each gets a receipt: expected vs observed)
Money and shares
- Deposit 0; deposit 1 wei of USDC (6-dec rounding → 0 shares? must revert clearly); deposit when supply==0 but treasury>0 (a direct donation): phase 5 quotes 1 USDC to 1e18 shares and permits genesis; the donation accrues to the first depositor. Earn this with a new receipt, not the former trap receipt.
- Ragequit with 0 shares; ragequit all shares of the last member (treasury fully paid out; DAO empty but alive); ragequit while a proposal is in voting and in grace (allowed); ragequit by a YES voter (verify Baal semantics).
- Treasury holding a second asset (MOCK from a strategy): Baal has no guildTokens registry; ragequit pays only the assets explicitly named in an ascending-address token list. Test direct second-asset payout and strategy unwind back to USDC.
Proposals and votes
- Vote after voting ends; vote twice; vote in the block of the submission (the relay must wait one block, direct callers get `TimePointNotDetermined`); `work` with < 1 share (self-sponsor reverts `!sponsor`, no task recorded); propose with < 1 share through the relay (409, no sponsored deployment); the same (template, params, member, salt) proposed twice (second quote finds the existing instance; the proposal must be refused or re-salted); process before grace ends; process twice; proposal whose multicall reverts (actionFailed=true: funds untouched); proposal that funds a contract with a bug that traps funds (stop() must still work; if the template cannot return, that is a template bug → fix).
- Spam: 100 proposals by one member; cost = gas only (offering 0); UI/relay must page.
- minRetention edge: exactly 34% deficit leaves the required 66% and passes; 33.9% passes; a deficit above 34% defeats the proposal. A return repairs only that same account's deficit.
- Governance config change to absurd values (voting 1 s, grace 0) by vote: allowed by design; document the consequence.
Templates
- Strategy: deadline passed before first run; run() spam by a stranger (must be harmless); MockDex price moves past take-profit and stop; migrate to a non-template contract by a passed proposal; an EOA is rejected by `NotAContract` with actionFailed and unchanged balances.
- Project: tranche with verifiers that never confirm (funds sit; stop() by vote returns them); topUp beyond treasury balance (multicall reverts, actionFailed); amend params mid-tranche.
- Payment: an ERC-20 transfer does not invoke a recipient receive hook. Pay a contract with no receive function and prove its USDC balance increases; separately prove an over-budget Payment becomes actionFailed with treasury unchanged.
Relay and adapter
- Replay of a signed intent; intent for another chain id; intent from an address with no code (T0 path); sponsor cap exhausted (clear error, agent can still self-pay); relay down (agents can use the contracts directly; README says how).
Beacon
- state.json includes proposals with states and deadlines; stale snapshot flag; README hash pinned.

## Exit criteria
All rows above have receipts in evidence/testnet/; a stranger agent (fresh key, no repo access) completes the cold-start path twice; PARAMETERS.md reviewed by the user line by line.

## Historical phase 3 status (2026-09-08; archived deployment `deployments/base-sepolia-phase3.json`)

The following receipts only prove the old contracts. They confer no phase 5 acceptance. Original verification files are retained under `deployments/verification-base-sepolia-phase3/`; original test logs remain in place.
| Step | Result | Evidence |
| --- | --- | --- |
| Deploy + genesis (50 mock USDC -> 50e18 shares) | done from the mini; cost 0.00014 ETH; a first attempt died at `Baal.setUp` (`GS104`) because the public RPC answered from a node behind `Safe.setup` (fixed: settled simulations and reads) | `deploy-base-sepolia-2026-09-08.log`, record `deployments/base-sepolia-phase3.json` |
| Source verification (Etherscan V2, chainid 84532) | 12 local + 6 upstream (Baal/Safe singletons, Safe proxy) verified | `verify-base-sepolia-2026-09-08.log`, `deployments/verification-base-sepolia-phase3/evidence/` |
| Relay + tunnel + beacon | `https://relay-zero.mkyang.ai` (launchd on the mini), `https://zero-one-beacon.vercel.app` (README 43 lines, validate PASS) | `state/logs/` on the mini; `beacon:validate` output in the commit message |
| Governance 120 s / 120 s (testnet only) | proposal #1 (Config template) submitted and voted at 2026-09-07T23:31Z under 6 h / 6 h; executable 2026-09-08T11:31:34Z; restored to 21600 / 21600 by proposal #145 (executed 2026-09-09 after #144, the orchestrator cold-start Payment; Baal processes in sponsorship order, decision.md 2026-09-08; log evidence/testnet/governance-restore-6h-2026-09-08.log) | `governance-120s-proposal-2026-09-08.log`, `governance-*` logs |
| Scenarios A-J on Sepolia (a fresh DAO each, 120 s / 120 s) | batch: A, B, C, D, E, F, I, J PASS; G and H failed on a stale `describe()` read from a lagging RPC node (the receipts show `Unwound` + `Completed`); `describe()` pinned to the head and G, H re-run PASS | `scenarios-A-J-base-sepolia-2026-09-08.log`, `scenarios-G-rerun-*.log`, `scenarios-H-rerun-*.log`, DAO records `scenario-daos/*.json` (failed attempts under `scenario-daos/failed/`) |
| Corner cases on fresh DAOs (money, trap, votes, absurd governance, templates) | 34 rows, all as expected, every row with a receipt; the votes part was re-run after two harness fixes (stale head, lagging getBlock) | `corner-cases-daos-base-sepolia-2026-09-08.log` + `corner-cases-daos-2026-09-08-money-trap-absurd-templates.json`, `corner-cases-daos-votes-rerun-base-sepolia-2026-09-08.log` + `corner-cases-daos-2026-09-08-votes.json`, DAO records `scenario-daos/corner-*.json` |
| Corner cases through the relay (replay, other chain id, undelegated, work/propose below threshold, duplicate instance, spam 100, burst, sponsor cap, beacon) | 11 rows PASS; the 100-proposal run is `*-spam100` (100 accepted in 2,608 s, 0 rate-limit waits; its paging check ran before the beacon stopped shadowing `/proposals.json`), the final run re-checks paging with 5 proposals (total 136, `?limit=10` -> 10, `nextBefore`) | `corner-cases-relay-main-2026-09-08-spam100.log` + `.json`, `corner-cases-relay-main-2026-09-08.log` + `corner-cases-relay-2026-09-08.json` |
| Cold start (README + relay only), mini and main Mac | T1 + T0 paths | `cold-start-mini-*.log`, `cold-start-main-*.log` |

Findings (details in decision.md phase 2c and docs/PARAMETERS.md): a period of 0 is "unchanged" for Baal and the Config
template then reverts `NotApplied` (minimum 1 s; 1 s / 1 s is terminal: the first proposal under it was already Defeated
after one block); `ragequit(0, 0)` is a no-op; a second asset is paid only when named, in ascending address order; a
vote in the submission block reverts `TimePointNotDetermined` (reproduced twice: submit and vote landed in one block);
exactly 34% leaving keeps a proposal alive, 37.7% defeats it; a Payment above the treasury ends `actionFailed` with the
Safe untouched; a strategy whose deadline passed before its first run unwinds on that run; a stop-loss run returns the
proceeds; the zero-supply trap is terminal and the relay reports `ZeroShares` without a transaction; an ERC-20 Payment
cannot be reverted by its recipient. Relay: a second proposal for an instance is refused (409), `/proposals.json` pages,
a burst is bounded by the 8-deep queue (503) before the 6/min window (429) on a ~10 s-per-request public RPC, the
sponsor cap answers 503 "send the transaction yourself".

Public-RPC facts that changed code (all in decision.md phase 2c): `sepolia.base.org` caps `eth_getLogs` at 10,000 blocks
(`-32614`), rate-limits bursts (`over rate limit`), and answers from nodes that lag a receipt by seconds. The relay index
now scans logs incrementally in 9,000-block chunks, batches reads through Multicall3, falls back to publicnode / drpc /
tenderly, and every script pins reads to a block it has seen.
